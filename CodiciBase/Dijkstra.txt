Solo pesi non negativi w>=0 
usa coda di priorità
fa in ordine

PERCORSO MINIMO DA UN NODO SORGENDE A TUTTI GLI ALTRI NODI 

DIJKSTRA(G,w,s)

//init source 
for each v in V[G]
    d[v] = infinito
    padre[v] = nil 

d[s] = 0

S = {}
Q = V[G] 

while Q non è vuota 

    u = extractMin(Q)
    S = S u {u}

    //relax 
    for each v in Adj[u]
        if d[v] > d[u] + w(u,v)
            d[v] = d[u] + w(u,v)
            padre[v] = u 
            DECREASE-KEY(Q,v,d[v])

----------------------------------------------

for each v in V[G]
    d[v] = infinito
    pi[v] = NIL

d[s] = 0

Q = V[G]
S = {}

while Q non è vuota 
    
    u = extractMin(Q)
    S = S u {u}

    for each v in Adj[u]
        if d[v] > d[u] + w(u,v)
            d[v] = d[u] + w(u,v)
            pi[v] = u 
