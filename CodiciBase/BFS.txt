BFS(G)

for each u in V[G] - {S}
    color[u] = white 
    padre[u] = NIL 
    d[u] = infinito

color[S] = gray 
d[S] = 0 
padre[s] = NIL 

Q = Coda vuota 
ENQUEUE(Q, s)

while(Q!= Vuoto)

    u = DEQUEUE(Q)
    
    for each v in Adj[u]
        if color[v] = white 
            color[v] = gray 
            padre[v] = u 
            d[v] = d[u] +1
            ENQUEUE(Q,v)
        
    color[u] = BLACK 

--------------------------------------------
for each v in V[G] - {s}
    color[u] = white 
    padre[u] = NIL 
    d[u] = infinito

d[s] = 0
color[s] = gray 
padre[s] = NIL 

Q = coda vuota 
ENQUEUE(Q,s)

while Q non è vuota 

    u = DEQUEUE(Q)

    for each v in Adj[u] 
        if color[v] == white
            color[v] = gray 
            d[v] = d[u] +1
            padre[v] = u 
            ENQUEUE(Q,v)

    color[u] = BLACK