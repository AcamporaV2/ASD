Dato un albero MinHeap H di grandezza Heapsize+1, si estragga il minimo e losi  cambi con l'ultimo elemento. 
Si scriva un algoritmo che prendendo in input H e la grandezza Heapsize, restituisce il numero di scambi in H necessari a ripristinare la proprietà dell'Heap.
Qualora l'albero soddisfi la priorità dell'Heap, l'algoritmo deve restituire valore 0



MinHeap

l = left(i)
r = right(i)

if l <= A.heapsize and A[l] < A[min]

    min = l
else 
    min = i 
    
if r<= A.heapsize and A[r] < A[min]
    min = r

if min !=i 
    swap(A[min], A[i])

    return 1 + MinHeap(A, min)
else 
    return 0