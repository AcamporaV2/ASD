dato un grafo orientato G(V,E) e un peso k, scrivere un algoritmo in pseudocodice che restituisce 
una Lista di nodi che hanno come distanza da un nodo sorgente s il cammino minimo pari a k.




DIJKSTRA 

for each v in V[G] 
    d[v] = infinito 
    pi[v] = NIL 

d[s] = 0 

S = insieme vuoto 
Q= V[G]


while Q non è vuota 

    u = extractmin(Q)

    S = S U {u}

    for each v in Adj[u]
        if d[v] > d[u] + w(u,v)
            d[v] = d[u] + w(u,v)
            pi[v] = u 

    if d[u] == k 
        INSERT(Nodi_pari,v)


return Nodi_pari