Numero scambi heapsort


Heapify(A,i)

l = left(i)
r = right(i)

if l <= A.heapsize AND A[l] < A[max]
    max = l 
else 
    max = i 

if r <= A.heapsize AND A[r] < A[max]
    max = radicato

if max !=i 
    swap A[i] A[max]
    Return 1 + Heapify(A,max)