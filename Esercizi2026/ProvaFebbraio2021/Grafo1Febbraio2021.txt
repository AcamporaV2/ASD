Dato un grafo orientato G=(V,E) non pesato, si scriva in pseudocodice un algoritmo che restituisca il numero dei cicli presenti nel grafo



DFS 

for each u in V[G]
    color[u] = white 
    padre[u] = NIL 

time = 0 
cicli = 0
for each u in V[G]
    if color[u] == white 
        DFS-VISIT(u)


return cicli 


DFS-VISIT(u, cicli)

color[u] = gray 
time = time + 1
d[u] = time 

for each v in Adj[u]
    if color[v] == White 
        padre[v]= u 
        DFS-VISIT(v)
    else if color[v] == gray
            cicli = cicli + 1

color[u] = black