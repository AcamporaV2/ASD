Dato una coda di min priorità, implementata mediante un heap, scrivere un algoritmo che restituisca, 
come output, il numero degli scambi necessari ad eseguire il metodo EXTRACT_MIN.

HEAP

EXTRACT-MIN-COUNT(A)
1  if A.heap-size < 1
2      error "heap underflow"
3  min = A[1]
4  A[1] = A[A.heap-size]      // non conta come scambio
5  A.heap-size = A.heap-size - 1
6  count = MIN-HEAPIFY-COUNT(A, 1)
7  return count

MIN-HEAPIFY-COUNT(A, i)
1  l = LEFT(i)
2  r = RIGHT(i)
3  if l ≤ A.heap-size and A[l] < A[i]
4      smallest = l
5  else smallest = i
6  if r ≤ A.heap-size and A[r] < A[smallest]
7      smallest = r
8  if smallest ≠ i
9      exchange A[i] with A[smallest]   // conta come scambio
10     return 1 + MIN-HEAPIFY-COUNT(A, smallest)
11 return 0


LEFT(i)
1  return 2i


RIGHT(i)
1  return 2i + 1