/*


Dato un grafo G=(V,E) aciclico orientato e pesato con funzione peso non negativa w: E->R+ 
ed un nodo s (appartenente) a V, si scriva un algoritmo in pseudocodice 
che restituisca i nodi foglia dell’albero dei cammini minimi radicato in s.



DIJKSTRA (G, w , s)
  for each v in V[G]
    d[v] <- infinito
    padre[v] <- NIL
  d[s] <- 0

 Q <- V[G]
 S <- insieme vuoto

while q != vuoto 
    u <- EXTRACT-MIN(Q)
    S = S U {u}
    for each v in Adj[U]
        if d[v] > d[u] + w(u,v)
            d[v] = d[u] + w(u,v)
            padre[v] = U


-Sopra parte DIJKSTRA, sotto calcolo

Foglie <- insieme vuoto
PadrePres[num_nodi] <- FALSE per ogni nodo in V[G]

for each u in v[G]
    if padre[u] != NIL
        PadrePres[padre[u]] <- TRUE
    
for each u in V[G]
    if d[u] < infinito and PadrePres[num_nodi] = FALSE 
        Foglie = Foglie U {u}

return foglie 

