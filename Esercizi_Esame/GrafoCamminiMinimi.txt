/*


Dato un grafo G=(V,E) aciclico orientato e pesato con funzione peso non negativa w: E->R+ 
ed un nodo s (appartenente) a V, si scriva un algoritmo in pseudocodice 
che restituisca i nodi foglia dell’albero dei cammini minimi radicato in s.



DIJKSTRA (G, w , s)

  for each v in V[G]
    d[v] <- infinito
    padre[v] <- NIL
  
  d[s] <- 0

 Q <- V[G]
 S <- insieme vuoto

while Q != vuoto 
    u <- EXTRACT-MIN(Q)
    S = S U {u}
    for each v in Adj[U]
        if d[v] > d[u] + w(u,v)
            d[v] = d[u] + w(u,v)
            padre[v] = U


-Sopra parte DIJKSTRA, sotto calcolo

Foglie <- insieme vuoto
Is_Parent[num_nodi] <- FALSE per ogni nodo in V[G]

// Marca tutti i nodi che SONO padri
for each u in V[G]
    if padre[u] ≠ NIL
        Is_Parent[padre[u]] <- TRUE

// Un nodo è foglia se è raggiungibile E non è padre di nessuno
for each u in V[G]
    if d[u] < ∞ and Is_Parent[u] == FALSE
        Foglie = Foglie ∪ {u}
        
return foglie 

